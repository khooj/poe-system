import { RequiredItem } from "@bindings/domain/bindings/RequiredItem";
import { NativeSelect, Select } from "@mantine/core";

export type ItemNameModProps = {
  item: RequiredItem
};

export const ItemNameMod = ({ item }: ItemNameModProps) => {
  return <Select

};
